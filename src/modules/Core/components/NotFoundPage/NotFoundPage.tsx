import React from 'react';
import './style.scss';

const NotFoundPage = () => {

	return (
		<div className="not-found-page">
			NotFoundPage
		</div>
	);
};

export default React.memo(NotFoundPage);
